import { Lobby } from 'boardgame.io/react';
import { BuracoGame } from './game.js';
import { BuracoBoard } from './Board.jsx';

// Dynamically uses your server's IP address
const SERVER_ADDRESS = `http://${window.location.hostname}:8000`;

const App = () => (
  <div style={{ padding: '50px', backgroundColor: '#111', minHeight: '100vh', fontFamily: 'sans-serif' }}>
    <Lobby
      gameServer={SERVER_ADDRESS}
      lobbyServer={SERVER_ADDRESS}
      gameComponents={[
        { game: BuracoGame, board: BuracoBoard }
      ]}
    />
  </div>
);

export default App;
