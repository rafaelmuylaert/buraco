import { Lobby } from 'boardgame.io/react';
import { BuracoGame } from './game.js';
import { BuracoBoard } from './Board.jsx';

// Your Linux server's IP address
const SERVER_ADDRESS = 'http://10.0.0.2:8000';

const App = () => (
  <div style={{ padding: '50px', backgroundColor: '#f0f0f0', minHeight: '100vh', fontFamily: 'sans-serif' }}>
    <Lobby
      gameServer={SERVER_ADDRESS}
      lobbyServer={SERVER_ADDRESS}
      gameComponents={[
        { game: BuracoGame, board: BuracoBoard }
      ]}
    />
  </div>
);

export default App;
