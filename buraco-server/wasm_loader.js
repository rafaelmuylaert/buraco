// WASM engine stub — the C++ engine needs to be rewritten to match the
// current segmented input architecture. JS engine is used in the meantime.
export async function initWasm() { return false; }
export function isWasmReady() { return false; }
